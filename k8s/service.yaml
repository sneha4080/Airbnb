apiVersion: v1
kind: Service

metadata:
  name: air-bnb-service
  namespace: default

spec:
  selector:
    app: air-bnb

  ports:
    - protocol: TCP
      port: 3003
      targetPort: 3003

  type: NodePort

  